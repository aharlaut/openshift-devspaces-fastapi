schemaVersion: 2.2.2
metadata:
  name: python-hello-world
components:
  - name: python
    container:
      image: python:3.9
      memoryLimit: '2Gi'
      memoryRequest: '1Gi'
      cpuLimit: '2'
      cpuRequest: '1'
      mountSources: true
commands:
  - id: install-dependencies
    exec:
      label: "Install dependencies the application"
      component: python
      workingDir: .
      commandLine: pip install -r requirements.txt
  - id: run
    exec:
      label: "Run the application"
      component: python
      workingDir: .
      commandLine: uvicorn hello-world:app --host 0.0.0.0 --port 5000
      group:
        kind: run
